<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />

<meta name="viewport" content="initial-scale=1.0, user-scalable=no" />
    <script type="text/javascript"
      src="https://maps.googleapis.com/maps/api/js?key=AIzaSyCNCaeiVel5_T12h-P2JP96JRJAoWPPYss&sensor=true">
    </script>

  <title><%= content_for?(:title) ? yield(:title) : "Contact Cake" %></title>

  <%= stylesheet_link_tag    "application" %>
  <%= csrf_meta_tags %>
</head>

<body>

<script>
  (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
  (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
  m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
  })(window,document,'script','//www.google-analytics.com/analytics.js','ga');

  ga('create', 'UA-47427172-2', 'contactcake.herokuapp.com');
  ga('send', 'pageview');

</script>



<% if !user_signed_in? %>
<nav class="top-bar" data-topbar>
  <ul class="title-area">
    <li class="name">
      <h1><%= link_to("Contact Cake", root_path) %></h1>
    </li>
    <li class="toggle-topbar menu-icon"><a href="#">Menu</a></li>
  </ul>

  <section class="top-bar-section">
    <!-- Right Nav Section -->
    <ul class="right">
      <li class="has-dropdown">
        <ul class="dropdown">
        </ul>
      </li>
    </ul>

    <!-- Left Nav Section -->
    <ul class="left">
      <li><%= link_to("Sign In", new_user_session_path) %></li>
      <li><%= link_to("Sign Up", new_user_registration_path) %></li>
      <li><%=link_to('Contact Us', new_user_inquiry_path) %></li>
    </ul>
  </section>
</nav>
<% end %>

<% if user_signed_in? %>
<nav class="top-bar" data-topbar>
  <ul class="title-area">
    <li class="name">
      <h1><%= link_to("Contact Cake", root_path) %></h1>
    </li>
    <li class="toggle-topbar menu-icon"><a href="#">Menu</a></li>
  </ul>

  <section class="top-bar-section">
    <!-- Right Nav Section -->
    <ul class="right">
        <li><%=link_to('Sign Out', destroy_user_session_path, method: :delete) %></li>
      <li class="has-dropdown">
        <ul class="dropdown">
        </ul>
      </li>
    </ul>

    <!-- Left Nav Section -->
    <ul class="left">
      <li><%= link_to("All Contacts", root_path) %></li>
      <li><%= link_to "New Contact", new_contact_path %></li>
      <li><%=link_to('Contact Us', new_user_inquiry_path) %></li>
    </ul>
  </section>
</nav>
<% end %>

  <% flash.each do |name, msg| %>
  <%= content_tag :div, msg, class: "#{name} alert-box" %>
  <% end %>

  <%= yield %>
  <%= javascript_include_tag "application" %>

<script>
  // To attach Backstrech as the body's background
  $(document).ready(function() {
    $.backstretch( "<%=image_path('background.jpg')%>");
  });
</script>
<br>

</body>
</html>






