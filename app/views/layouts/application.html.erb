<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />

  <title><%= content_for?(:title) ? yield(:title) : "Contact Cake" %></title>

  <%= stylesheet_link_tag    "application" %>
  <%= javascript_include_tag "vendor/modernizr" %>
  <%= csrf_meta_tags %>
</head>

<body>

  <nav class="top-bar" data-topbar>
        <ul class="title-area">
        <li class="name">
          <h1><%=link_to('Contact Us', new_user_inquiry_path) %></h1>
        </li>
          <li class="toggle-topbar menu-icon"><a href="#">Menu</a></li>
        </ul>
    <% if user_signed_in? %>
    <ul class="title-area">
      <li class="name">
        <h1><%=link_to('Sign Out', destroy_user_session_path, method: :delete) %></h1>
      </li>
    </ul>
    <section class="top-bar-section">
      <!-- Right Nav Section -->
      <ul class="right">
        <li class="active"><%=link_to("current user: #{current_user.email}", root_path) %></a></li>
      <li class="has-dropdown">
         <li class="has-dropdown">
        <ul class="dropdown">
        </ul>
      </li>
    </ul>
    <!-- Left Nav Section -->
    <ul class="left">
      <li class="has-form">
        <div class="row collapse">
<!--           <div class="large-8 small-12 columns">
            <input type="text" placeholder="Find Facts">
          </div>
          <div class="large-4 small-3 columns">
            <a href="#" class="alert button expand">Search</a>
          </div> -->
            <%= form_tag contacts_path, method: :get do %>
              <%= text_field_tag :query %>
              <%= submit_tag 'Search', class: 'alert button expand' %>
            <% end %>
          </div>
        </div>
      </li>
      </ul>
    </section>
    <% end %>
  </nav>

  <% flash.each do |name, msg| -%>
  <%= content_tag :div, msg, class: "#{name} alert-box" %>
  <% end %>

  <div class='center'>
    <%= yield %>
    <%= javascript_include_tag "application" %>
  </div>

</body>
</html>









