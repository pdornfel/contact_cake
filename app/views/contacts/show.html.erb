<div>
  <div class="panel callout radius">
    <h1><%= link_to "#{@contact.name}", root_path %></h1>
  </div>

    <ul>
      <%= image_tag(@contact.contact_photo,size: "250x250")  %>
    </ul>
    <div class='panel callout radius'>
      <h5><%= link_to "All Contacts", contacts_path %></h5>
      <h5><%= link_to "Edit Contact", edit_contact_path %></h5>
      <h5><%= link_to "Add Fact", new_contact_fact_path(@contact) %></h5>
    </div>

    <div class='panel callout radius'>
      <h5>Facts:</h5>
      <% @contact.facts.each do |fact| %>
        <h4><%= fact.fact %></h4>
            <%= link_to "Edit Fact", edit_contact_fact_path(@contact.id, fact) %>
      <% end %>
    </div>
  </div>








